.cache
.DS_Store
.idea
*.log
*.tgz
coverage
dist
lib-cov
logs
node_modules
temp
packages/ts-cloud/bin/cloud
/docs/.bunpress
.env
bin/cloud
# Management-dashboard secrets (never commit): the legacy Basic-auth
# credentials, the user store (scrypt password hashes + site grants), and the
# session signing key. Ignore the whole local state directory - anything
# ts-cloud writes there is per-machine, and some of it is a credential.
.ts-cloud/
/pantry
pantry
/ui/.stx

# TypeScript incremental build cache (caused stale typecheck masking)
*.tsbuildinfo

.e2e-scratch/
